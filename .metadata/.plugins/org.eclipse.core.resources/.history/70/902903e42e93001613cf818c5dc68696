package sensor;

public class SurfaceFactory {
	public static Surface getSurface(String type, int x, int y){
		if(type.equals("bare")){
			return SurfaceHighPileImpl(type,x,y);
		}
		if(type.equals("low-pile")){
			return SurfaceLowPileImpl(type,x,y);
		}
		if(type.equals("high-pile")){
			return SurfaceLowPileImpl(type,x,y);
		}
		return null;
	}

	


}
